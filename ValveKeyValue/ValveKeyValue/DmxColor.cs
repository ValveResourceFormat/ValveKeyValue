namespace ValveKeyValue
{
    /// <summary>
    /// Represents a DMX color with red, green, blue, and alpha components.
    /// </summary>
    public readonly record struct DmxColor(byte R, byte G, byte B, byte A);
}
